// the set time function help us to delay the execution of the specific 
// part of the code, this can help us to bring pop or notification, at 
// some important point later on during porgramming the code 

// syntax of settime out  setTimeout(function, milliseconds);

function aware() 
{
    console.log("time is indeed precious, but foucsed time is more precious then normal one");
}

setTimeout(aware,1000); 

console.log('those who dont value time, they know the half of that knowledege');
